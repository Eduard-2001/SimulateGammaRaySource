To produce simulations, use the ./Simulation/Simulation_randPSF.ipynb.

you can choose to add a random modification to the LHAASO PSF to examine the robustheit of the ML model.

you can add some point sources to the skymap. 

the original sky map is convolved with a gaussian PSF with adjustable sigma as the target image. 